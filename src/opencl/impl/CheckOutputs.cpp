#include "opencl/Context.hpp"

namespace mlp {
    namespace opencl {
        void Context::checkOutputs(const Matrix& outputs, const std::vector<int>& labels, Buffer& correct, Buffer& classifications, const size_t samples) const {
            
        }
    }
}